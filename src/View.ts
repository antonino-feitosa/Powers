/*
import {Rect} from './Maps';

function fieldOfView<T>(matrix: T[], x:number, y:number, radius:number, opaque: (cell:T) => boolean) {
    
    let angle = Math.atan2(radius, 1);
    let rect: 

}
*/